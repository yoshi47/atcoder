use proconio::input;

fn main() {
    input! {
        x[[usize]],
    }
    let mut (a1, a2, a3) = (0, 0, 0);
    let mut (b1, b2, b3) = (0, 0, 0);
    while a1 + b1 <= x[0][0] {
        while a1 <= b1 {
            
        }
    }
}
